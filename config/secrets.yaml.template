# Copy this file to secrets.yaml and replace the API key
# DO NOT commit secrets.yaml to version control!

openai:
  # Get your API key from: https://platform.openai.com/api-keys
  api_key: "your-actual-api-key-here"
  
  # Optionally override the default model from example.yaml
  # model: "gpt-4"  # uncomment to use a different model
